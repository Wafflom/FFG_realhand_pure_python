[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "realhand-pure-python-teleop"
version = "0.1.0"
description = "Pure Python RealForce and RealMCG teleoperation helpers for RealHand hands."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pyserial",
    "PyYAML",
]

[tool.setuptools.packages.find]
include = ["realhand_pure_python*"]

[tool.setuptools.package-data]
realhand_pure_python = [
    "config/*.yml",
    "assets/robots/hands/real_hand/*/*.urdf",
]
